{
  "name": "mj-live",
  "version": "0.1.0",
  "description": "MJ Live — real-time AI cognitive pipeline demo on SAP BTP CAP + Solace.",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "hana"
      }
    }
  }
}
