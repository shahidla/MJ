{
  "name": "mj-live-cap",
  "version": "0.1.0",
  "description": "MJ Live — CAP AI cognitive pipeline: LangChain + Claude + HANA Vector + Solace",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "langchain": "^0.3.0",
    "@langchain/anthropic": "^0.3.0",
    "@langchain/community": "^0.3.0",
    "solclientjs": "^10",
    "axios": "^1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "dev": "cds watch"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "hana",
        "pool": {
          "max": 10,
          "acquireTimeoutMillis": 15000
        }
      },
      "auth": {
        "kind": "dummy"
      }
    }
  }
}
